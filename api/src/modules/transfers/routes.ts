import { FastifyInstance } from 'fastify'

export async function transferRoutes(fastify: FastifyInstance) {
  // Transfer routes are handled in the credits module
  // This is kept for future expansion if needed
}
